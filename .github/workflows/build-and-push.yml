name: Build and Push ALT Linux Docker Images

on:
  push:
    branches: [ main, master ]
    paths:
      - 'releases.txt'
      - 'build.sh'
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild even if tag exists in registry'
        required: false
        type: boolean
        default: false
      preset:
        description: 'Preset to build (minimal|samba|virt|web|graph)'
        required: false
        default: 'minimal'

jobs:
  select-runner:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    outputs:
      runner_type: ${{ steps.check.outputs.runner_type }}
    steps:
      - name: Check if phylex runner is online
        id: check
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN || secrets.GH_PAT || secrets.PAT || secrets.GITHUB_TOKEN }}
        run: |
          RESPONSE=$(gh api "repos/${{ github.repository }}/actions/runners" 2>/dev/null || true)
          STATUS=$(echo "$RESPONSE" | jq -r '.runners[]? | select(.name=="phylex") | .status' 2>/dev/null || echo "offline")
          if [ "$STATUS" = "online" ]; then
            echo "Runner phylex is ONLINE."
            HAS_LABEL=$(echo "$RESPONSE" | jq -r '.runners[] | select(.name=="phylex") | .labels[]? | select(.name=="phylex") | .name' 2>/dev/null || true)
            if [ "$HAS_LABEL" = "phylex" ]; then
              echo 'runner_type=["self-hosted", "phylex"]' >> "$GITHUB_OUTPUT"
            else
              echo 'runner_type=["self-hosted"]' >> "$GITHUB_OUTPUT"
            fi
          else
            echo "Runner phylex is not available ($STATUS). Falling back to ubuntu-latest."
            echo 'runner_type=["ubuntu-latest"]' >> "$GITHUB_OUTPUT"
          fi

  build-and-push:
    needs: select-runner
    runs-on: ${{ fromJSON(needs.select-runner.outputs.runner_type) }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          if command -v apt-get &>/dev/null; then
            sudo apt-get update -qq || true
            sudo apt-get install -y -qq squashfs-tools curl tar gzip p7zip-full || true
          elif command -v dnf &>/dev/null; then
            sudo dnf install -y -q squashfs-tools curl tar gzip p7zip || true
          fi

      - name: Login to Docker Hub
        if: env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME || secrets.DOCKER_USERNAME || 'runalsh' }}
          password: ${{ secrets.DOCKERHUB_TOKEN || secrets.DOCKER_PASSWORD || secrets.DOCKER_TOKEN }}
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN || secrets.DOCKER_PASSWORD || secrets.DOCKER_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Images
        env:
          PUSH_TO_DOCKERHUB: ${{ secrets.DOCKERHUB_TOKEN != '' || secrets.DOCKER_PASSWORD != '' || secrets.DOCKER_TOKEN != '' }}
          PUSH_TO_GHCR: true
          SKIP_EXISTS_CHECK: ${{ github.event.inputs.force_rebuild || false }}
        run: |
          PRESET="${{ github.event.inputs.preset || 'minimal' }}"
          ./build.sh --preset "$PRESET"

      - name: Always Clean Up Leftover Mounts, Files and Docker Images
        if: always()
        run: |
          sudo umount -f /tmp/altlinux-iso-patch_* 2>/dev/null || true
          sudo rm -rf /tmp/altlinux-iso-patch_* 2>/dev/null || true
          docker images --format '{{.Repository}}:{{.Tag}}' | grep -E '^((ghcr\.io/)?runalsh/altlinux-patch)(:|$)' | xargs -r docker rmi -f 2>/dev/null || true
